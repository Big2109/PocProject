@* @using Poc.Extensions
@using Poc.Models
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>

    <!-- FontAwesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">

    <!-- Bootstrap CSS (opcional, se ainda usar algumas classes) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Tailwind CSS via Vite -->
    <link rel="stylesheet" href="~/dist/main.css" />
</head>

<body class="overflow-x-hidden
    overflow-y-auto
    h-screen
    font-roboto
    bg-gradient-to-b
    from-[#05032e]
    via-[#0f0d40]
    to-[#24234a] text-white">

    <header class="sticky top-0 w-full z-[9999]">
        <nav class="bg-gradient-to-b from-white via-white to-[#e8e7df]">
            <div class="container-fluid">
                <a class="text-center break-words" asp-controller="Home" asp-action="Index" title="Home">
                    <i class="logo"></i>
                </a>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar"
                    aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse justify-content-between" id="mainNavbar">
                    <ul class="navbar-nav flex-grow-1"></ul>

                    <div>
                        @if (User.Identity != null && User.Identity.IsAuthenticated)
                        {
                            <span class="me-3">
                                <i class="me-2 fas fa-circle-user"></i>@UserSessionHelper.Nome
                            </span>
                            <a class="btn btn-primary text-white" asp-area="" asp-controller="Login"
                                asp-action="Sair">Sair</a>
                        }
                        else
                        {
                            <a class="btn btn-primary text-white" asp-area="" asp-controller="Login"
                                asp-action="Index">Login</a>
                        }
                    </div>
                </div>
            </div>
        </nav>
    </header>

    <!-- Apps Vue -->
    @if (UserSessionHelper.IsLogged)
    {
        <div id="MenuApp"></div>
    }

    <div id="ParticlesApp"></div>

    <div id="modals-root">
        <FeedbackModal ref="feedbackModal" />
        <ConfirmacaoModal ref="confirmacaoModal" />
    </div>

    <main role="main" class="pb-3">
        @RenderBody()
    </main>

    <footer class="footer text-muted py-4">
        <div class="d-flex justify-center text-center text-gray-700">
            &copy; 2025 - Eric Marques Bighi - <a class="underline" asp-area="" asp-controller="Home"
                asp-action="Privacy">Privacy</a>
        </div>
    </footer>

    <!-- Scripts -->
    <script type="module" src="~/dist/main.bundle.js"></script>

    @await RenderSectionAsync("Scripts", required: false)

    @if (TempData["ExibirModal"] != null)
    {
        <script>
            const exibirModal = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(TempData["ExibirModal"]));

            if (exibirModal === "feedback") {
                const modalDataFeedback = {
                    tipo: "@TempData["TipoModal"]",
                    mensagem: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(TempData["MensagemModal"])),
                    camposInvalidos: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(TempData["CamposInvalidos"]))
                                        };

                window.showFeedbackModal(modalDataFeedback.tipo, modalDataFeedback.mensagem, modalDataFeedback.camposInvalidos);
            }
            else if (exibirModal === "confirmacao") {
                const modalDataConfirmacao = {
                    mensagem: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(TempData["MensagemModal"]))
                                        };

                window.showConfirmacaoModal(modalDataConfirmacao.mensagem);
            }
        </script>
    }
</body>

</html> *@

@*
Nome	Hex	Uso
Gray 900	#121212	Fundo principal da página
Gray 800	#1E1E1E	Seções secundárias, cards
Gray 700	#2C2C2C	Background de elementos menores
Gray 600	#3A3A3A	Bordas, divisores, menus
Gray 500	#525252	Texto secundário / placeholders
Gray 400	#737373	Texto menos importante
Gray 300	#A3A3A3	Texto leve / legendas *@

<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sua Landing Page</title>
    @* <link href="~/dist/main.css" rel="stylesheet" /> *@
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body
    class="overflow-x-hidden overflow-y-auto h-screen font-roboto bg-gradient-to-b from-[#121212] via-[#2C2C2C] to-[#3A3A3A] text-white">
    <!-- Header -->
    <header class="w-full bg-gradient-to-b from-[#3A3A3A] via-[#3A3A3A]">
        <div id="HeaderApp"></div>
        @* <nav>
            <div>
                <a class="text-center break-words" asp-controller="Home" asp-action="Index" title="Home">
                    <img src="~/img/logo/logotipo.gif" class="pl-3 w-[90px] h-[75px]" alt="Logo">
                </a>

                <button type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar"
                    aria-expanded="false" aria-label="Toggle navigation">
                    <span>
                        <i class="fas fa-bars"></i>
                    </span>
                </button>

                <div class="collapse navbar-collapse justify-content-between" id="mainNavbar">
                    <ul class="navbar-nav flex-grow-1"></ul>

                    <div>
                        @if (User.Identity != null && User.Identity.IsAuthenticated)
                        {
                            <span class="me-3">
                                <i class="me-2 fas fa-circle-user"></i>@UserSessionHelper.Nome
                            </span>
                            <a class="btn btn-primary text-white" asp-area="" asp-controller="Login"
                                asp-action="Sair">Sair</a>
                        }
                        else
                        {
                            <a class="btn btn-primary text-white" asp-area="" asp-controller="Login"
                                asp-action="Index">Login</a>
                        }
                    </div>
                </div>
            </div>
        </nav>
        <div class="max-w-6xl mx-auto flex justify-between items-center py-4 px-6"> *@

        @* <h1 class="text-2xl font-bold">MinhaMarca</h1>
            <nav class="hidden md:flex gap-6"> <a href="#features" class="hover:text-blue-600">Recursos</a> <a
                    href="#pricing" class="hover:text-blue-600">Preços</a> <a href="#contact"
                    class="hover:text-blue-600">Contato</a>
            </nav>

            <nav :class="{'hidden': !menuOpen, 'flex': menuOpen}" class="flex-col gap-6 md:hidden">
                <a href="#features">Recursos</a>
                <a href="#pricing">Preços</a>
                <a href="#contact">Contato</a>
            </nav>

            <button class="md:hidden">☰</button> *@
        </div>
    </header> <!-- Hero Section -->
    <section class="mt-16 px-6">
        <div class="max-w-6xl mx-auto flex flex-col md:flex-row items-center">
            <div class="md:w-1/2">
                <h2 class="text-4xl md:text-5xl font-bold mb-6">A solução ideal para transformar seu negócio</h2>
                <p class="text-lg mb-6">Construa sua presença digital com uma página rápida, moderna e totalmente
                    responsiva.</p> <button
                    class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition"> Começar Agora
                </button>
            </div>
            <div class="md:w-1/2 mt-10 md:mt-0"> <img src="https://placehold.co/500x350" class="rounded-lg shadow-lg"
                    alt="Imagem ilustrativa"> </div>
        </div>
    </section> <!-- Features -->
    <section id="features" class="mt-24 px-6">
        <div class="max-w-6xl mx-auto text-center mb-12">
            <h3 class="text-3xl font-bold mb-4">Recursos Poderosos</h3>
            <p class="text-gray-600">Tudo o que você precisa para crescer rápido.</p>
        </div>
        <div class="grid md:grid-cols-3 gap-10 max-w-6xl mx-auto">
            <div class="p-6 bg-white shadow rounded-lg">
                <h4 class="text-xl font-semibold mb-3">⚡ Performance</h4>
                <p>Páginas ultrarrápidas otimizadas para conversão.</p>
            </div>
            <div class="p-6 bg-white shadow rounded-lg">
                <h4 class="text-xl font-semibold mb-3">📱 Responsivo</h4>
                <p>Design perfeito em qualquer dispositivo.</p>
            </div>
            <div class="p-6 bg-white shadow rounded-lg">
                <h4 class="text-xl font-semibold mb-3">🔒 Seguro</h4>
                <p>Segurança e estabilidade de alto nível.</p>
            </div>
        </div>
    </section> <!-- Pricing -->
    <section id="pricing" class="mt-24 px-6">
        <div class="max-w-6xl mx-auto text-center mb-12">
            <h3 class="text-3xl font-bold mb-4">Planos e Preços</h3>
            <p class="text-gray-600">Escolha o plano ideal para você.</p>
        </div>
        <div class="grid md:grid-cols-3 gap-10 max-w-6xl mx-auto">
            <div class="p-8 border rounded-xl text-center bg-white">
                <h4 class="text-2xl font-bold mb-4">Básico</h4>
                <p class="text-4xl font-bold mb-4">R$ 49<span class="text-lg">/mês</span></p>
                <ul class="mb-6 text-gray-600">
                    <li>✔ Acesso limitado</li>
                    <li>✔ Suporte padrão</li>
                    <li>✔ 5 páginas</li>
                </ul> <button class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition"> Escolher
                    Plano </button>
            </div>
            <div class="p-8 border-2 border-blue-600 rounded-xl text-center bg-blue-50">
                <h4 class="text-2xl font-bold mb-4">Profissional</h4>
                <p class="text-4xl font-bold mb-4">R$ 99<span class="text-lg">/mês</span></p>
                <ul class="mb-6 text-gray-700">
                    <li>✔ Acesso completo</li>
                    <li>✔ Suporte prioritário</li>
                    <li>✔ 20 páginas</li>
                </ul> <button class="bg-blue-700 text-white px-6 py-3 rounded-lg hover:bg-blue-800 transition"> Escolher
                    Plano </button>
            </div>
            <div class="p-8 border rounded-xl text-center bg-white">
                <h4 class="text-2xl font-bold mb-4">Premium</h4>
                <p class="text-4xl font-bold mb-4">R$ 199<span class="text-lg">/mês</span></p>
                <ul class="mb-6 text-gray-600">
                    <li>✔ Acesso ilimitado</li>
                    <li>✔ Consultoria dedicada</li>
                    <li>✔ Páginas ilimitadas</li>
                </ul> <button class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition"> Escolher
                    Plano </button>
            </div>
        </div>
    </section> <!-- Contact -->
    <section id="contact" class="mt-24 px-6 mb-24">
        <div class="max-w-3xl mx-auto text-center mb-10">
            <h3 class="text-3xl font-bold mb-4">Entre em Contato</h3>
            <p class="text-gray-600">Dúvidas? Fale com a gente!</p>
        </div>
        <form class="max-w-3xl mx-auto grid gap-6 bg-white p-8 rounded-xl shadow"> <input type="text"
                placeholder="Seu nome" class="border p-3 rounded w-full"> <input type="email" placeholder="Seu email"
                class="border p-3 rounded w-full"> <textarea placeholder="Sua mensagem"
                class="border p-3 rounded w-full"></textarea> <button
                class="bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 transition"> Enviar Mensagem </button>
        </form>
    </section>
    <main> @RenderBody() </main>
    <footer class="text-center py-6 text-gray-600 border-t"> © 2025 MinhaMarca — Todos os direitos reservados </footer>
    @await RenderSectionAsync("Scripts", required: false)
    <script type="module" src="~/dist/main.bundle.js"></script>
</body>

</html>